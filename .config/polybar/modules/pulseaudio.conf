[module/pulseaudio]
type = internal/pulseaudio
format-volume = <ramp-volume> <label-volume>
format-font = 4
label-muted = "  muted"

ramp-volume-font = 3
ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 


; leave empty to use default sink so it can be switched
sink =

use-ui-max = false
interval = 5

click-right = pavucontrol &